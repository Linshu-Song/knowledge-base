---
sidebar_position: 1
title: Optimizers Comparison
description: Overview of common PyTorch optimizers like SGD, Adam, and AdamW.
---

# Optimizers Comparison

Optimizers are algorithms or methods used to change the attributes of a neural network such as weights and learning rate in order to reduce the losses. Optimization algorithms or strategies are responsible for reducing the losses and providing the most accurate results possible.

In PyTorch, all optimization algorithms are encapsulated in the `torch.optim` package.

## Basic Usage

To use an optimizer, you must instantiate it by passing an iterable containing the parameters (tensors that should be optimized) and optionally setting optimizer-specific options like the learning rate (`lr`).

```python
import torch.optim as optim

# Instantiate the optimizer
optimizer = optim.Adam(model.parameters(), lr=1e-3)

# Inside the training loop
optimizer.zero_grad()  # 1. Clear old gradients
loss.backward()        # 2. Compute new gradients (backpropagation)
optimizer.step()       # 3. Update parameters
```

:::danger[Critical Step]
Always call `optimizer.zero_grad()` before computing the loss gradients (via `loss.backward()`). By default, gradients are accumulated in PyTorch. Failing to clear them will lead to incorrect updates.
:::

## Common Optimizers

The choice of optimizer significantly impacts the model's convergence speed and final accuracy.

### Stochastic Gradient Descent (SGD)

The classic algorithm. It updates parameters in the opposite direction of the gradient of the objective function. It often benefits from **Momentum**, which helps accelerate gradients in the right direction, dampening oscillations.

```python
# SGD with Nesterov momentum
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.9, nesterov=True)
```

### Adam

Adam (Adaptive Moment Estimation) computes individual adaptive learning rates for different parameters from estimates of first and second moments of the gradients. It's often the default choice for many deep learning tasks due to its rapid initial convergence.

```python
# Standard Adam optimizer
optimizer = optim.Adam(model.parameters(), lr=0.001, betas=(0.9, 0.999), eps=1e-08)
```

### AdamW

A variant of Adam that decouples weight decay from the gradient update. It has been empirically shown to yield better generalization performance than Adam, especially in Transformer models and large vision models.

```python
# AdamW is heavily preferred over Adam for modern architectures
optimizer = optim.AdamW(model.parameters(), lr=1e-4, weight_decay=1e-2)
```

## Comparison Table

| Optimizer | Pros | Cons | Best Used For |
| :--- | :--- | :--- | :--- |
| **SGD (+Momentum)** | Often finds a flatter, more generalizable minimum. | Requires careful tuning of learning rate and momentum. Slower initial convergence. | Image Classification, ResNets. |
| **Adam** | Fast convergence. Less sensitive to learning rate tuning. | Generalization can sometimes be worse than SGD. | GANs, standard MLPs, rapid prototyping. |
| **AdamW** | Fixes weight decay regularization in Adam. Excellent generalization. | Requires careful tuning of `weight_decay`. | Transformers (NLP/Vision), state-of-the-art models. |

## Parameter Groups

You can define different learning rates or hyperparameters for different parts of the network by passing an iterable of dictionaries to the optimizer.

```python
# Finetuning example: 
# Train the classifier with a larger learning rate, freeze or train base model slowly
optim.SGD([
    {'params': model.base.parameters(), 'lr': 1e-4},
    {'params': model.classifier.parameters(), 'lr': 1e-2}
], momentum=0.9)
```

## References

- [PyTorch Official Documentation: torch.optim](https://pytorch.org/docs/stable/optim.html)
- [Decoupled Weight Decay Regularization (Loshchilov & Hutter, 2017)](https://arxiv.org/abs/1711.05101)
